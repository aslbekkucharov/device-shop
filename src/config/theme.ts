import type { ThemeConfig } from "antd"

export const themeConfiguration: ThemeConfig = {
    token: {
        borderRadius: 8,
        colorPrimary: '#000000'
    },

    components: {
        Select: {
            optionSelectedBg: '#e2e8f0'
        }
    }
}